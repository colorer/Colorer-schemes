name: ci
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo "${{ github.actor }}"
      - run: ${{ github.token }}
    env:
      NOTE: |
        block
      FOLDED: >
        line
