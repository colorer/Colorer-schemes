package foo.bar.TestPackage
{
    public class TestClass extends Sprite
    {
        public function TestClass(i:int)
        {
            var func:*=function(vars:*)
            { // this is a comment
            }
        }

        private static var someVector : __AS3__.vec.Vector.<int>;
    }
}
